
logging:
  path: c:/trace/logs
  level:
    root: INFO
    org:
      springframework:
        web: DEBUG
        security: DEBUG

spring:
  datasource:
    url: jdbc:h2:mem:trace
    username: root
    password: changeme
    driver-class-name: org.h2.Driver
    schema: classpath:h2/schema.sql
    data: classpath:h2/data.sql
  h2.console:
    enabled: true
    path: /h2-console

#management:
#  security:
#    roles: ADMIN
#    enable: false

trace:
  # 质检报告文件保存路径
  static:
    upload: c:/trace/static/upload/